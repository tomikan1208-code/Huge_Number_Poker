# 巨大数ポーカー — カードUI・数式ビルダー 改良プロンプト

## 0. 目的とスコープ
既存実装（index.html / css/style.css / js/engine.js / js/game.js / js/ui.js / js/online-game.js）に対する改修プロンプト。対象は以下の3点。ゲームルール本体・通信部分（server.js等）は変更しない。

1. 手札・演算子カードのビジュアルデザイン刷新
2. 数式構築UIをテキスト入力主体からドラッグ&ドロップ主体へ刷新
3. 実装調査で判明した既存の不具合2件の修正

優先度は P0（最優先・不具合） > P1（デザイン・D&D本体） > P2（任意・追加提案）。

---

## 1.【P0】不具合修正（2件）

### 1-A. べき乗（^）が厳密計算されず常に近似値になる
- **現象**: `5^3` のような小さい値でも厳密値「125」ではなく「約10^2.0969」のような対数近似表示になる。`^`を使った数式は、仕様上「厳密比較」されるべきところが常に「桁数一致判定」に回ってしまっている。
- **原因**: `js/engine.js` の `HugeNumber.power()` 内、指数を`Number`型に変換した変数`e`に対して `e & 1n` / `e >>= 1n` というBigInt専用の演算をかけており、型混在の例外が`try/catch`で握り潰されて常にlog10近似へフォールバックしている。
- **修正方針**: ループ変数をBigIntに統一する（例: `let e = BigInt(exp);` にする、あるいは`other.exact`を直接ループカウンタとして使う）。
- **影響範囲**: `js/engine.js`はローカル/ソロ/オンライン全モードで共有されているため、修正は全モードに反映される。

### 1-B. 手札カードが重複して使用できてしまう
- **現象**: 計算フェーズでカードをクリックして数式に追加した後、同じカードを再度クリックするとさらに数式に追加できてしまう（1枚のカードは本来1回しか使えないはず）。
- **原因**: `js/ui.js` の手札描画・クリックハンドラ（`renderHand()` / `renderSoloMode()` 内、および `addCardToFormula()`）が、クリック時に`selectedCards.has(card.id)`を確認していない。CSSの`.card.selected`も見た目（枠色・光彩・浮き上がり）だけで、クリック自体を無効化していない。
- **修正方針**: 使用済みカードは手札内で無効化表示にし、ドラッグ開始自体をできない状態にする。下記2-3のD&D実装で「配置エリアから手札へ戻す」操作をすれば再度使用可能に戻る、という状態管理にする。

---

## 2.【P1】カードUI・数式ビルダーの全面刷新

### 2-1. 画面レイアウト（下から上へ3層構造）
現在の「手札→タブ→テキストエリア→プレビュー」という縦積みを、以下の3層構造に変更する。

```
┌─────────────────────────────┐
│                             │
│        数式プレビュー          │ ← 上段：大きく・中央・枠なし（この計算フェーズの主役）
│                             │
├─────────────────────────────┤
│   ドラッグ&ドロップ配置エリア     │ ← 中段：数式構築キャンバス
├─────────────────────────────┤
│   手札（横並び）               │ ← 下段：現状維持
└─────────────────────────────┘
```
プレビューは「操作の結果としてのご褒美」、手札は「素材」という役割の違いを画面上の重み（サイズ・位置）でも表現する。

### 2-2. カードのビジュアルデザイン
現状「色が強すぎる／小さい／無機質」「演算子カードに枠がない」という点への対応。
- **彩度・トーン**: 現状の`--card-green/blue/red/purple/orange`はやや原色寄りで彩度が高いので、少し落ち着いた深みのあるトーンに調整し、光り方（box-shadow等）で強弱をつける。
- **サイズ**: 一回り拡大する。
- **質感**: 単色塗りつぶし＋角丸だけで終わらせず、タロット/トランプのように「内側に一段細い縁取りのフレーム線」を入れる、微細なグラデーションやノイズ/グレインで陰影を足す、四隅に小さな装飾（コーナー飾り）を入れるなど、"1枚の道具"としての質感を持たせる。演算子カードは特にこのフレームを明確に。
- **数字カードとの差別化**: 数字カードは白背景でシンプルという現行方針は維持しつつ、番号の書体をやや個性のあるもの（等幅数字や少し癖のある数字書体）にして、演算子カードの色付きフレームと対比させる。

### 2-3. ドラッグ&ドロップによる数式構築（デフォルト挙動に変更、クリックでの追加は廃止）
- 「テキスト」「ビジュアル」タブのうち、**「ビジュアル」を初期状態のデフォルトタブ**にする。
- 操作は**ドラッグ操作のみ**とする。現行の「カードをクリックすると末尾に追記される」方式は廃止する。
- 配置エリアは、演算子ノードに左右スロットが生成されるような木構造ではなく、**手札ゾーンから配置エリアへカードを1列に並べていくだけの、シンプルな横並びのシーケンス**とする。数字・演算子とも同じ1列に並び、テキストの数式文字列と1対1で対応する構造にする（＝「演算子を置くと枠が出てくる」という仕組みは廃止）。
- **括弧は「(」「)」それぞれ独立した専用カード**として用意し、他の数字・演算子カードと同様にこの列へドラッグして挿入できるようにする。ただしどちらも手札の5枚制限やデッキ消費とは無関係な、無制限に使える常設ツールとして表示する（既存仕様の「括弧は自由に使用可能」を踏襲。5枚制限のカウント対象外）。

**列への挿入・並べ替えの挙動（重要）**
- カードをドラッグして列の上に持っていくと、**掴んでいる間、既存カードがリアルタイムで左右にズレて隙間を作り**、「ここで離すとこの位置に挿入される」ことが視覚的にわかるようにする（挿入位置のライブプレビュー）。
- 挿入位置は、ドラッグ中のカードの水平位置と既存カード同士の中間点との近さで判定する。**列からどれだけ離れた場所でドロップしても、直近の妥当な挿入位置に自動的に吸い寄せられて（スナップして）挿入される**。ドロップ位置ぴったりに置く必要はない。
- 端に挿入した場合は、置いた瞬間に**列全体が新しい幅に合わせて中央へ寄る**（配置エリアの中で常に列が中央に整列された状態を保つ）。
- 挿入・並べ替えのたびに、置いた場所にそのまま留まるのではなく、**全カードが新しい並び位置へ滑らかに移動するアニメーション**を入れる（FLIP的な手法＝移動前後の位置差分をtransformで補間する方式が実装しやすい）。
- 既に列にあるカードをつまんで別の位置へドラッグすれば、同じ挙動で**並べ替え**もできる。列から手札ゾーンへドラッグして戻せば、その1枚は列から削除され、再度使用可能な状態で手札に戻る（1-Bの状態管理と連動）。
- 配置エリアのシーケンスをテキスト側の数式文字列と引き続き双方向同期する（内部表現は共通のまま）。

参考: 配置エリアの内部状態は木構造ではなく、単純な**順序付き配列**で表現できる（テキストの数式文字列と実質同じ情報量）。

```typescript
type PlacedCard = {
  instanceId: string;                                // この列の中での一意なID(並べ替え・削除の追跡用)
  card: Card | { type: 'paren'; value: '(' | ')' };  // 数字・演算子カード、または括弧トークン
};

type BuilderState = {
  sequence: PlacedCard[];   // 左から右への並び。この配列をそのままテキスト表現へ変換する
};
```

### 2-4. 数式プレビューの組版刷新
現状の生テキスト＋`<sup>`/`<sub>`だけの表示が「そっけない」という点への対応。
- 計算フェーズの主役はこのプレビューなので、テキスト入力欄のような枠線・背景ボックスは撤去し、背景に溶け込む形で大きく中央に表示する。
- 数字書体を、本文用のUIフォントとは別に「見せ場用」の書体に切り替える（存在感のあるセリフ/スラブセリフ系や、やや癖のある数字書体など）。累乗・順列の上付き/下付きは、ブラウザ標準の`<sup>`任せの小さいサイズではなく、比率・位置を調整して読みやすく見栄えよく配置する。
- 可能であれば数式組版ライブラリ（KaTeX等）の導入も検討し、独自CSSでの微調整より正確な組版を狙う。
- 数式が完成した瞬間に軽く光る/拡大するなど、「大きな数ができた」ことを実感できる一瞬の演出があってもよい（やりすぎない程度に）。

---

## 3.【P2】任意の追加提案

- **演算子の優先順位**: 現状 `*`・`^`・`↑↑` が同順位・左結合という独自仕様になっており、標準数学とは異なる（例: `2*3^2` は実装上36だが、標準数学なら18）。標準的な優先順位（`^` > `*` > `+`）に合わせるか、意図的な独自ルールとして仕様書に明記するかを判断してほしい。
- **ソロモードの核心機能が未実装**: 元の仕様書10.1にある「全ての有効な組み合わせを自動探索してランキング表示」「最適解に対する比率(%)」が未実装（現状は自分の入力した1式を自己採点するのみ）。今回のD&D改修とは独立して着手可能。
- **アクセシビリティ**: D&D化に伴い、キーボード操作やタップだけでも数式を組める代替手段を残す。フォーカスの可視化、`prefers-reduced-motion`への配慮も。
- **細かい表示統一**: ソロ画面中央の「POT: ∞」表示を、ヘッダー側と同じ「—」に統一する（見た目上の整合性のみの軽微な修正）。
