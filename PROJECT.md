# 巨大数ポーカー（Huge Number Poker）— 仕様・構造まとめ

3本のプロンプト（初期仕様 / フィードバック / UI改良）と、現在のソースコードを突き合わせて整理したドキュメント。
**「仕様」＝プロンプトで要求されている内容、「実装」＝現行コードの実際の挙動**として区別して記述する。

- 初期仕様: `巨大数ポーカー（Huge Number Poker）— 実装プロンプト.md`
- フィードバック: `user_pasted_clipboard_long_content_as_file_# 巨大数ポーカー（Huge Numb(1).txt`
  （※ 初期仕様とほぼ同一内容で、セクション9で末尾が切れている。実質的な追加指示はなく、初期仕様の再掲）
- UI改良: `巨大数ポーカー_UI改良プロンプト.md`（カードUI刷新 / D&Dビルダー / 不具合2件）

---

## 1. ゲーム概要

ポーカーの駆け引き（チップ・ベット・ブラフ）と、配られたカードから数式を組んで**できるだけ大きい数**を作るパズルを融合させたゲーム。

- 各プレイヤーに **7枚**配札
- 7枚のうち **5枚以下**を選んで数式を構築
- **カード連結禁止**（`2` と `3` を並べて `23` にはできない。1枚＝数式内の1要素）
- ショーダウンで**数式の評価値が最大**のプレイヤーが勝利。同値なら山分け
- プレイヤーは**自分で計算結果を入力**する。システムの計算値と一致しなければ **×（失格）** ＝ 計算ミスがそのままリスクになる

---

## 2. カード構成

### 2.1 デッキ（1組あたり／`js/game.js: buildDeck()`）

| 種別 | 内容 | 枚数 |
|---|---|---|
| 数字カード | `2`〜`9` を各4枚（♠♥♦♣） | 32枚 |
| 演算子 `+` | 加算（緑） | 4枚 |
| 演算子 `*`（表示 `×`） | 乗算（青） | 4枚 |
| 演算子 `^` | べき乗（赤） | 4枚 |
| 演算子 `!` | 階乗（紫・単項後置） | 4枚 |
| 演算子 `P` | 順列 ₙPᵣ（橙） | 4枚 |
| Joker `↑↑` | テトレーション（黒金・特別デザイン） | 2枚 |

合計 **54枚 / 1デッキ**。デッキ数は設定で 1〜3 組まで指定可能（`deckCount`）。

### 2.2 括弧カード

`(` `)` は**手札とは別の常設ツール**。デッキを消費せず、無制限に使え、**5枚制限のカウント対象外**（UI改良プロンプト 2-3）。
実装では `index.html` / `online.html` の `.paren-tools` に固定表示され、D&Dで列に挿入する。

---

## 3. 演算子仕様

| カード | 内部記号 | 表示 | 種別 | 動作 |
|---|---|---|---|---|
| `+` | `+` | `+` | 二項 | 加算 |
| `×` | `*` | `×` | 二項 | 乗算 |
| `^` | `^` | 上付き指数 | 二項 | べき乗 |
| `!` | `!` | `!` | 単項（後置） | 階乗 |
| `P` | `P` | 左下・右上付き | 二項 | 順列 ₙPᵣ = n!/(n−r)! |
| Joker | `↑↑` | `↑↑` | 二項 | テトレーション |

### 3.1 全角→半角の自動変換（`FormulaEvaluator.normalize()`）

`＋→+` / `×・＊・Ｘ・ｘ→*` / `＾→^` / `！→!` / `Ｐ・ｐ→P` / 単独の `↑ → ↑↑`
※ 内部処理は常にASCII、表示層のみ `×` や `<sup>`/`<sub>` を使う。

### 3.2 演算子の優先順位（**現行実装の実際**）

`js/engine.js` の `FormulaParser` の文法は以下：

```
expr   := term   (('+' | 'P') term)*        ← 最も弱い・左結合
term   := power  ('*' power)*                ← 左結合
power  := factor (('^' | '↑↑') power)*       ← 最も強い・右結合
factor := number | '(' expr ')' | factor '!'
```

つまり **`^` `↑↑` ＞ `*` ＞ `+` `P`**。`^` と `↑↑` は右結合（`2^3^2 = 2^(3^2)`）。

> UI改良プロンプト §3（P2）が指摘していた「`*`・`^`・`↑↑` が同順位・左結合」は修正済み。
> `P` を `+` と同順位・左結合に置いているのは独自仕様として維持し、**ルール画面に明記した**
> （`2+3P2 = (2+3)P2 = 20`、`5*2P3 = (5*2)P3`）。

### 3.3 数式ルール

- カード1枚＝1要素。連結禁止
- 括弧は自由に使用可
- 使用カードは5枚以下（括弧を除く）
- 構文エラーの数式は無効 → ショーダウンで ×
- **式作成エリア以外**（プレビュー・ショーダウン・ログ・履歴）では常に**数学的組版**で表示
  - `5^3` → 5³ / `5P3` → ₅P³ / `5*3` → 5 × 3 / `(5+3)^2` → (5 + 3)²
  - aria-label に読み上げテキストを併記（`FormulaEvaluator.toSpeechText()`）

---

## 4. ゲーム設定（`GameConfig` / `js/game.js: DEFAULT_CONFIG`）

| 項目 | キー | 既定値 |
|---|---|---|
| 初期チップ | `initialChips` | 1000 |
| 小ブラインド | `smallBlind` | 10 |
| 大ブラインド | `bigBlind` | 20 |
| アンティ | `ante` | 5 |
| ベット制限時間（通常） | `betTimeLimit` | 10秒 |
| ベット制限時間（親） | `dealerTimeLimit` | 20秒 |
| レベルアップ間隔 | `levelUpHands` | 5ハンド |
| デッキ数 | `deckCount` | 1 |
| プレイヤー数 | （UI側） | 2〜6 |
| システムが自動計算する | `autoCalcMode` | OFF |
| 計算フェーズの下限/上限 | `minCalcTime` / `maxCalcTime` | 30秒 / 600秒 |

- **自動連動**: 初期チップを変更すると SB/BB/アンティが比率維持で自動追従（1000→2000 なら 10/20/5 → 20/40/10）。手動で上書きすると以降は追従しない（`window._blindManualOverride`）
- 設定は LocalStorage キー `huge-number-poker-settings` に保存・復元
- ソロスコアは `huge-number-poker-solo-scores` に保存（上位20件）

---

## 5. ゲームフロー（ステートマシン）

```
SETTING → DEALING → BETTING_1 → EXCHANGE → BETTING_2 → CALCULATION → SHOWDOWN → SETTLEMENT
                        ↑                                                            │
                        └────────────────── 次ハンド ────────────────────────────────┘
```

### 5.1 DEALING（配札）
アンティを全員から徴収 → SB/BB を強制ベット → 各プレイヤーに7枚配札。ディーラーボタンは毎ハンド時計回りに移動。

### 5.2 BETTING_1 / BETTING_2（ベットラウンド）
アクションは **フォールド / チェック / コール / レイズ / オールイン**。
- 制限時間: 通常プレイヤー `betTimeLimit`、**親は `dealerTimeLimit`**（配札直後の確認時間を確保するため）
- 時間切れ → **コール不要ならチェック、必要ならフォールド**（無条件フォールドだと理不尽なため）
- ラウンド終了条件は「全員がアクション済み **かつ** ベット額が揃った」。
  レイズが入ると他プレイヤーの `hasActed` がリセットされるので、**大ブラインドのオプション**も成立する
- アクティブが1人になった時点で即ショーダウンへ
- ブラインドはラウンド1の開始時にリセットされず、そのまま場のベットとして扱われる。
  アンティは `anteBet` に分離されており、コール額の計算には混ざらない

### 5.3 EXCHANGE（交換）
アクティブな各プレイヤーが手札から任意の枚数（0〜7枚）を選んで交換。捨てた枚数だけ山札から引く。ターン制ではなく「準備完了」方式。

### 5.4 CALCULATION（数式構築・結果入力）
- **入力時間 = ポットの合計チップ数（秒）**。極端な値を避けるため 30〜600秒に丸める
- 手順: 使用カードを D&D で並べる（5枚以下・0枚不可）→ **答えの入力形式ガイドを見て**答えを入力 → 確認ダイアログ → 提出
- 時間切れは `finishCalculation()` が未提出者を失格にしてショーダウンへ。
  ローカルはクライアント側タイマー、オンラインは**サーバー側タイマー**が締切を持つ
- `autoCalcMode: ON` の場合は結果の手動入力を省略し、システムが自動計算。○×判定なし・構文エラーのみ失格（純粋な数式構築ゲームになる）

### 5.5 SHOWDOWN（ショーダウン）
フォールドしていない全員について、使用カード / 数式（組版表示）/ プレイヤー入力値 / システム計算値 / ○× を公開。
- 並び順は **評価値の降順** → 失格 → 未提出 → フォールド（比較は `HugeNumber.compare()`）
- **×は失格**（計算ミス or 構文エラー）、時間切れの未提出も失格
- ○の中で**評価値が最大**のプレイヤーが勝者。同値なら山分け

### 5.6 SETTLEMENT（精算）
- ポットを勝者で山分け（端数切り捨て、余りは次ポットへ持ち越し）
- 勝者ゼロ（全員失格）ならポットは丸ごと次ハンドへ持ち越し
- チップ0のプレイヤーは脱落。生存1人でゲーム終了
- `levelUpHands` ごとに SB/BB/アンティを2倍にしてレベルアップ

---

## 6. 計算エンジンの設計（`js/engine.js`）

### 6.1 どこまで計算するか — 3層表現

`HugeNumber` は値を3つの層で保持し、層ごとに「何が保証されるか」を変える。

| 層 | `kind` | 保持形式 | 範囲 | 保証されること |
|---|---|---|---|---|
| 厳密層 | `exact` | `BigInt` | 10進 **1000桁以下** | 値そのものが完全に正しい |
| 実数層 | `real` | `double` | 絶対値 **10¹⁵ 未満** | 主に対数など非整数の中間値 |
| タワー層 | `tower` | `{height, top}` | それ以上すべて | 値 = 10^(10^(…^top))、10 が `height` 個 |

タワー層は `top ∈ [1,10)` に正規化される。**`height` 自体も `HugeNumber`** なので、
`9↑↑9↑↑9` のように「タワーの高さが 9↑↑9」という値も再帰的に表現できる。

境界は `HUGE_LIMITS` に定数としてまとまっている（`EXACT_MAX_DIGITS` / `TOWER_MIN` ほか）。

### 6.2 比較 — 直接計算できない数どうしの順序

`compare()` は **高さ → 先頭指数** の辞書順で決める。

```
10^(10^100)  →  height=3, top≈2.0
10↑↑10       →  height=10, top≈1.0     ← 高さが大きいので必ずこちらが上
```

高さの比較は再帰的に `compare()` を呼ぶので、ペンテーション級でも順序が決まる。
`3↑↑1 < 3↑↑2 < 3↑↑3 < …` の狭義単調性はテストで検証済み。

**精度の限界（意図的な仕様）**: `top` は double なので有効数字は約15桁。
そのため高さ4以上の領域では、1段下の演算（`+` `×` `!`）が値を変えない。
例えば `9↑↑9` と `9↑↑9+2` は同一の値として扱われ、ショーダウンでは**同値＝山分け**になる。
有限精度である以上避けられないので、「差が精度限界未満なら同値」をルールとして採用している
（仕様の「同値なら山分け」と整合する）。

### 6.3 演算の実装方針

| 演算 | 厳密に計算する条件 | それを超えたとき |
|---|---|---|
| `add` | 両方 exact | 対数域で `log10(a+b) = log10(a) + log10(1+10^-d)`。差が17桁以上なら大きい方に吸収 |
| `multiply` | 桁数の和が1000以下 | `log10(ab) = log10 a + log10 b` |
| `power` | 指数 ≤ 10⁶ かつ結果1000桁以下 | `log10(a^b) = b · log10 a` |
| `factorial` | 結果が1000桁以下 | スターリング近似 `n(log₁₀n − log₁₀e) + ½log₁₀(2πn)` |
| `permutation` | r ≤ 20万 かつ結果1000桁以下 | `Σ log10(n−i)`、r も巨大ならスターリングの差 |
| `tetration` | 高さ ≤ 16 は実際に反復 | 巨大な x では `a^x ≈ 10^x` なので、残りの回数を**タワーの高さに加算**する |

### 6.4 正誤判定 — 規模に応じて申告形式が変わる

`FormulaEvaluator.judgeDeclaration()` が、システム値の層から**要求する答えの形式を自動的に決める**。
プレイヤーには入力欄の上（`#declaration-hint`）にその都度表示される。

| モード | 発生条件 | 要求される答え | 例 |
|---|---|---|---|
| `exact` | 厳密層 | 値そのもの | `125` |
| `digits` | 桁数が厳密に確定できる | 桁数 | `14401桁` |
| `scale` | 桁数すら書けない | 規模 | `10^10^30`、`10↑↑5` |

`parseDeclaration()` が受け付ける書式:
`125` / `1234桁`・`1234けた`・`1234 digits` / `1.5e30` / `2×10^30` / `10^10^100` / `10↑↑5`
（全角数字・全角記号・カンマ区切りも自動で正規化）

入力中は `#declaration-preview` に「システムがどう解釈したか」がリアルタイムで出る。

---

## 7. モード

| モード | 入口 | 状態管理 |
|---|---|---|
| **ローカル（1端末）** | `index.html` → `js/ui.js` | クライアント内の `Game` インスタンス |
| **オンライン対戦** | `online.html` → `js/online-*.js` + `server.js` | **サーバー権威**。Socket.io で同期 |
| **ソロ（スコアアタック）** | `index.html` のソロモード | ポーカー要素なし・時間無制限 |

### 7.1 オンライン対戦
- 4文字の部屋コードで参加。ホストが設定してゲーム開始（2人以上必要）
- 観戦者参加あり。ホスト切断時は自動でホスト継承
- 同名での再入室（reconnect）に対応。全員切断後30分で部屋削除
- サーバーは各クライアントに**自分の手札のみ**を送り、他人の手札は `?` にマスクして配信

### 7.2 ソロモード（仕様 10.1）
7枚から5枚以下で最大の数式を作り、答えを入力して自己採点。自己ベストと比較できる。
**仕様上は「全ての有効な組み合わせを自動探索して大きい順にランキング表示」「最適解に対する比率(%)」まで要求されているが、これは未実装**（現状は自分の1式を採点するのみ）。

なお、オンラインの `game.players` は脱落してもインデックスが変わらない（`isEliminated` フラグ管理）ので、
`room.players[i]` との対応が常に保たれる。手札のマスク判定はこの対応に依存している。

---

## 8. ファイル構造

```
Huge_Number_Poker/
├── index.html                  ローカル/ソロ用の全画面（ホーム・設定・ソロ・ルール・ゲーム）
├── online.html                 オンライン対戦用（ロビー＋ゲーム画面）
├── ai-lab.html                 AIテスト場（CPUの正答率を確かめる。§12.10）
├── AI学習コントロールパネル.vbs  学習GUIを開く（pythonw でコンソールなし。§13.5）
│                                ★UTF-16LE(BOM付き)で保存すること。UTF-8だと無言で起動しない
├── AI学習コントロールパネル.bat  同上（コンソールあり・Python と依存の確認つき）
├── server.js                   Express + Socket.io。部屋管理・サーバー権威のゲーム進行
├── package.json                deps: express, socket.io / `npm start` で server.js
├── start.bat                   起動一式（クリーンアップ→依存確認→FW確認→サーバ起動→ブラウザ）
├── allow-firewall.ps1          TCP 3000 の受信を許可（LAN内対戦用・管理者権限が必要）
├── share-internet.bat / .ps1   Cloudflare Tunnel で公開（遠くの友達と対戦する用）
├── game-ui-template-skill.md   UIフレームワーク定義（全体のデザイン基準）
├── css/
│   ├── style.css               全スタイル（:root のCSS変数、カード、D&D、ショーダウン等）
│   └── ai-lab.css              AIテスト場のレイアウトのみ（カードの見た目は style.css を流用）
├── js/
│   ├── engine.js               HugeNumber / FormulaParser / FormulaEvaluator（全モード共有）
│   ├── game.js                 Game クラス・デッキ構築・フェーズ制御（クライアント/サーバ共有）
│   ├── builder.js              FormulaBuilder（D&D数式ビルダー。全モード共有）
│   ├── audio.js                効果音（配布・チップ・勝利・エラー等）
│   ├── ui.js                   ローカル/ソロモードのUI制御
│   ├── online-manager.js       Socket.io 接続ラッパ
│   ├── online-lobby.js         ロビー画面（部屋作成・参加・設定）
│   ├── online-game.js          オンライン対戦のゲーム画面UI
│   └── ai-lab.js               AIテスト場の制御（既存APIを呼ぶだけ。ゲームロジックは持たない）
└── node_modules/
```

### 8.1 モジュール依存

```
engine.js ──┬── game.js ──┬── ui.js（index.html）
            │             └── server.js（Node側で require）
            ├── builder.js ─┬── ui.js
            │               └── online-game.js
            └── online-game.js（online.html）
```

`engine.js` と `game.js` はブラウザ／Node 双方から読める形（末尾で `module.exports` を条件付きエクスポート）。
**engine.js の修正は ローカル・ソロ・オンライン の全モードに波及する。**

### 8.2 主要データモデル

```typescript
type Card = {
  id: string;                      // 'n0', 'o12' など
  type: 'number' | 'operator';
  value: number | string;          // 2-9 | '+' '*' '^' '!' 'P' '↑↑'
  display: string;                 // 表示文字（'*' は '×'）
  suit?: string;                   // 数字カードのみ
  color?: string; label?: string;  // 演算子カードのみ
};

type Player = {
  id, name, chips, hand: Card[],
  isDealer, isActive,              // isActive:false = フォールド
  currentBet, totalBet,
  formula: FormulaSubmission|null,
  hasSubmitted, isAllIn, isReady,
};

type FormulaSubmission = {
  usedCards: Card[];
  formulaString: string;           // 正規化済み ASCII 表記
  playerDeclaredResult: string;
  systemEvaluatedResult: string|null;
  isCorrect: boolean;              // 入力値とシステム値が一致
  isValid: boolean;                // 構文として有効
};

// D&Dビルダーの内部状態（木構造ではなく順序付き配列）
type PlacedCard = { instanceId: string; card: Card | {type:'paren', value:'('|')'} };
type BuilderState = { sequence: PlacedCard[] };
```

---

## 9. UI設計（UI改良プロンプト準拠）

### 9.1 計算フェーズは下から上へ3層構造

```
┌─────────────────────────┐
│    数式プレビュー          │ 上段：枠なし・大きく中央（このフェーズの主役）
├─────────────────────────┤
│  D&D配置エリア             │ 中段：横1列のシーケンス
├─────────────────────────┤
│  括弧ツール ( )            │ 常設・無制限
├─────────────────────────┤
│  結果入力 [___] [提出]      │
├─────────────────────────┤
│  手札（横並び7枚）           │ 下段：素材
└─────────────────────────┘
```

対応DOM: `.main-workspace` > `.preview-area` / `.builder-area` / `.paren-tools` / `.result-input-row`、および `.hand-area`。

### 9.2 D&Dビルダー（`js/builder.js`）
- **操作はドラッグのみ**。旧来の「クリックで末尾に追記」は廃止
- 木構造ではなく**単純な横1列のシーケンス**。テキストの数式文字列と1対1対応
- ドラッグ中は挿入位置インジケーター（`.insert-indicator`）を表示し、列から離れた位置でドロップしても**直近の妥当な位置にスナップ**
- 列内のカードをつまめば**並べ替え**、手札ゾーンへ戻せば**削除して再使用可能**に
- 使用済みカードは手札で `.used` になりドラッグ不可
- 5枚制限のチェックは括弧を除いてカウント

### 9.3 カードデザイン
- 数字カード: 白背景・大きな数字・内側フレーム・四隅のコーナー飾り（`.card-corner`）
- 演算子カード: 色分け（緑/青/赤/紫/橙）＋内側フレーム＋グラデーション、Joker は黒金＋アニメーション
- 選択・使用状態は枠線発光／浮き上がりで表現

---

## 10. 修正履歴と現在の状態

2026-08-09 に、サーバーを起動してブラウザのコンソールを確認し、エンジンを Node で実行して
全項目を検証したうえで修正した。テストは 52 項目すべて成功。

### 10.1 【S級】起動直後から機能が死んでいた3件 — すべて修正済み

| # | 内容 | 原因と対応 |
|---|---|---|
| **S-1** | オンライン対戦が全く動かない | `online-lobby.js` と `online-game.js` が両方トップレベルで `const om` を宣言しており、クラシックスクリプトの共有字句スコープで `SyntaxError: Identifier 'om' has already been declared` になり、**online-game.js が1行も実行されていなかった**。→ `online-game.js` 全体を **IIFE で包み**、内部変数名も `mgr` に変更。以後この種の衝突は構造的に起きない |
| **S-2** | ローカル/ソロで「提出」「次のハンドへ」が無反応 | UI改良で HTML から消した `formula-text` などを JS が参照し続けており、`initEventListeners()` が `TypeError` で中断していた。→ タブ・テキスト入力まわりの残骸（`switchTab` / `handleFormulaInput` / `renderVisualBuilder` / `addCardToFormula` ほか）を**削除**。死んだ参照はゼロ |
| **S-3** | D&D の挿入位置が常に無視され必ず末尾に追加 | `_updateInsertIndicator()` が `insertIndex` を代入した直後に `_clearInsertIndicator()` で `null` に戻していた。→ 挿入位置を**ドロップ時の座標から確定**する方式に変更（ドラッグ中の状態に依存しない）。合わせて FLIP アニメーションも実装 |

### 10.2 【A級】ゲームルールが壊れていた5件 — すべて修正済み

| # | 内容 | 対応 |
|---|---|---|
| **A-1** | テトレーションが全滅（`3↑↑1` `3↑↑2` `3↑↑3` が同値） | `height === 2n` という Number×BigInt の厳密比較が常に false だったのが原因。計算エンジンを**タワー表現で再設計**し、高さを正しく扱うようにした。`3↑↑2=27` `3↑↑3=7625597484987` `2↑↑4=65536` を検証 |
| **A-2** | 組版表示が実際の計算と食い違う（`5^2*3` が 5⁽²ˣ³⁾ と表示） | 文字列走査による組版をやめ、**AST から描画**するようにした。パーサと同じ木を使うので原理的に食い違わない。括弧の補完も優先順位から自動で決まる |
| **A-3** | ブラインドが場のベットとして機能しない | `startBettingRound(1)` が `currentBet` を 0 にリセットしていたのをやめた。アンティは `anteBet` に分離してコール額計算に混ざらないようにした。初手を **BB の次**に修正し、`hasActed` 管理で **BB のオプション**も成立するようにした |
| **A-4** | ポットの持ち越しが必ず消える | `settle()` の結果を `carryOver` に退避し、`_startNewHand()` が **`pot = carryOver`** で開始するようにした。勝者なしのポットも山分けの端数も次ハンドへ正しく繰り越される |
| **A-5** | オンラインで脱落するとプレイヤー対応がずれる | `settle()` が `players` 配列を `filter` で詰めていたのをやめ、**`isEliminated` フラグ管理**に変更。`game.players[i]` と `room.players[i]` の対応が常に保たれるので、手札のマスクやアクション経路が入れ替わらない |

### 10.3 併せて対応したもの

- **巨大数の計算方針を全面的に再設計**（§6）。3層表現・タワー比較・規模に応じた申告形式
- **答えの入力形式ガイド**を新設。数式を組むたびに「厳密／桁数／規模」のどれで答えるかを表示
- **計算フェーズのタイマー**を実装（ローカル）。時間切れで `finishCalculation()` により未提出者を失格に
- **サーバー側フェーズタイマー**を実装。放置・切断で部屋が永久に止まる問題を解消
- **XSS**: プレイヤー名・ログをすべて `textContent` 経由に変更。サーバー側でも `sanitizeName()` で
  タグ・制御文字を除去し12文字に制限（`start.bat` が ngrok で公開する構成のため）
- **5枚制限の統一**: サーバー側検証も「数字＋演算子の合計 ≤ 5、括弧は対象外」に統一（従来は数字のみ数えていた）
- **カード生成の共通化**: `createStandardCardElement()` に一本化。オンラインでも数字カードの装飾が効き、
  括弧カードが「?」で表示される問題も解消
- **手札が見えない問題**: 計算フェーズ以外で手札が描画されていなかったのを修正
- **`.builder-empty` の重複・増殖**を解消（初期化のたびに増えていた）
- **山札切れ時の捨て札リシャッフル**を実装
- **`prefers-reduced-motion`** に対応。キーボードでの交換カード選択も可能に
- **設定値のサーバー側バリデーション**（範囲外の値を弾く）
- **自動計算モード**のラベルを「システムが自動計算する」に統一し、UI 分岐も実装

### 10.4 残っている未実装・既知の制限

- **ホットシートのプレイヤー交代**: `ui.js` の `currentPlayerIdx` は 0 固定。他プレイヤーは時間切れの
  自動アクションで進む。「1端末で複数人が順番に操作する」動線は未実装
- **ソロモードの全探索ランキング**（仕様 10.1-4/5）: 最適解の探索・最適解比率(%) は未実装。
  現状は自分の1式を採点し、自己ベストと比較するのみ
- **サイドポット**（仕様 8-2 で優先度低と明記）
  ※ **CPU 対戦は実装済み**（§12）。オンライン対戦への CPU 参加は未対応
- **ゲーム途中経過の復元**: 保存されるのは設定とソロスコアのみ
- **配札・チップ移動のアニメーション**（仕様7）は未実装
- **精度の限界**: 高さ4以上のタワー域では 1段下の演算が値を変えない（§6.2 参照）。仕様として明示済み
- **`P` の優先順位**: `+` と同順位・左結合という独自仕様。ルール画面に明記した
  （`2+3P2 = (2+3)P2 = 20`）

## 11. 起動方法とネットワーク

### 11.1 起動

```bash
npm install
npm start
```

Windows では **`start.bat`** をダブルクリックすれば、ポート3000のクリーンアップ →
依存関係の確認 → ファイアウォール確認 → サーバー起動 → ブラウザ起動までを一括で行う。
起動時に「この PC の URL」と「同じ LAN の他端末から使う URL」を表示する。

**中身は `tools/start.js`（Node）にある。`start.bat` は ASCII だけの薄い入口。**
日本語を含む UTF-8 の `.bat` は `chcp 65001` と噛み合わず、cmd がファイル位置を
見失って行の途中から実行してしまうため（§13.5 の表を参照）。
`.bat` に日本語のメッセージを書き戻さないこと。

- この PC: http://localhost:3000
- 同じ LAN の他端末: `http://<LANのIP>:3000/online`

### 11.2 遊ぶ相手ごとの構成

| 相手 | 必要なもの | 手順 |
|---|---|---|
| **この PC だけ** | なし | `start.bat` → http://localhost:3000 |
| **同じ Wi-Fi / LAN** | ファイアウォールの受信許可 | `start.bat` の案内で許可、または `allow-firewall.ps1` を管理者実行 |
| **遠くの友達** | Cloudflare Tunnel | **`share-internet.bat`** を実行して、出てきた URL を友達に渡す |

### 11.3 同じ LAN で遊べない場合（ファイアウォール）

Windows の受信規則は**実行ファイルのパス単位**で作られる。
このマシンには `…\nvm\v20.18.2\node.exe` に対する許可があったが、実際に動いているのは
`C:\Program Files\nodejs\node.exe` だったため許可が効かず、他端末から繋がらなかった。

`allow-firewall.ps1` は node のパスではなく **TCP 3000 というポート**に対して規則を作るので、
node のインストール先が変わっても効き続ける。

```powershell
# 許可する（管理者権限が必要）
powershell -ExecutionPolicy Bypass -File allow-firewall.ps1

# 元に戻す
powershell -ExecutionPolicy Bypass -File allow-firewall.ps1 -Remove
```

### 11.4 遠くの友達と遊ぶ（インターネット公開）

`share-internet.bat` が **Cloudflare Tunnel のクイックトンネル**を張る。ngrok は使わない。

- アカウント登録・認証トークン不要（`cloudflared` が未導入なら winget で自動インストール）
- HTTPS で公開され、WebSocket に対応しているので Socket.io がそのまま動く
- **ルーターのポート開放は不要**（PC から外向きに接続するだけ）
- 発行 URL は `https://xxxx.trycloudflare.com/online` の形。クリップボードにコピーされる

注意点:

- URL は起動のたびに変わる
- ウィンドウを閉じると公開は止まる
- **URL を知っている人は誰でも入れる**ので、SNS などに貼らないこと

### 11.5 公開時の安全対策

インターネットに出す前提で、サーバーは次のようにしてある。

- **静的配信を限定**: `/css` `/js` と `index.html` / `online.html` のみ。
  以前は `express.static(__dirname)` でプロジェクト全体を配信しており、
  `server.js`・`package.json`・`node_modules`・プロンプトの `.md` まで読めてしまっていた。
  現在は該当パスがすべて 404（パストラバーサルも遮断済み）
- **プレイヤー名のサニタイズ**: サーバー側で `sanitizeName()` がタグ・制御文字を除去し12文字に制限
- **設定値の範囲チェック**: ホストが送る設定を `startGame()` でクランプ
- **手札の秘匿**: サーバーは各クライアントに自分の手札のみ送り、他人の手札は `?` にマスク
- **フェーズタイマー**: 放置・切断で部屋が止まらないようサーバー側が締切を持つ

なお `index.html` は静的ファイルとしても動くので、オンラインを使わないなら
`index.html` を直接ブラウザで開くだけでもローカル/ソロは遊べる。

---

## 12. AIシステム（CPU / 対戦相手）

### 12.0 中心にある考え

**難易度を決めるのは「値の大きさ」ではなく「計算過程」である。**

`(4+6)^9 = 10^9` は `6^9 = 100777696` より値は大きいが、人間にとっては圧倒的に易しい。
前者は `4+6=10` という既知事実の検索と「1のあとに0を9個」という*パターンの書き取り*で終わる。
後者は 36→216→1296→7776→… と中間結果を作業記憶に載せたまま多桁の乗算を繰り返す。

| 式 | 値 | 所要時間 | 作業記憶ピーク | 上級の正答率 |
|---|---|---|---|---|
| `(4+6)^9` | 10⁹ | 8秒 | 2.5 | **0.92** |
| `6^9` | 約1.0×10⁸ | 33秒 | 3.9 | **0.52** |

### 12.1 構成

| ファイル | 役割 |
|---|---|
| `js/ai-cognition.js` | AST →「人間にとっての難易度」→ 正答率。誤答時の誤差分布も持つ |
| `js/ai.js` | 数式の全数列挙、(値,難易度) のパレート境界、申告文字列の生成、ヒューリスティック方策 |
| `js/ai-policy.js` | 観測（特徴量）の定義、行動空間、学習済み方策の推論 |
| `js/ui.js` | CPU の手番を進める（何をするかは上の3つに任せる） |

`ai-policy.js` は**学習と実プレイの唯一の接点**。ここの `buildObservation` を片方だけ
書き換えると「学習時と本番で入力が違う」という一番気づきにくい壊れ方をする。

### 12.2 難易度モデル（`ai-cognition.js`）

**前提は暗算ではなく「紙とペンでの手計算」**。人間のプレイヤーが紙に筆算を書くので、
CPU もそれに揃える。これが効くところが4つある。

| | 暗算モデル（旧） | 手計算モデル（現行） |
|---|---|---|
| 見直し | できない。誤ったら終わり | **できる。時間を積めば正答率が上がる** |
| 速度 | 速い（4桁×4桁を16秒） | 遅い（同 27秒。書く時間が支配的） |
| 1操作あたりのミス率 | 高い（同 26%） | 低い（同 10%。中間結果が紙に残る） |
| 作業記憶の超過 | 正答率を大きく削る | **主に時間を食う**（紙が外部記憶になる） |

一番効くのは1つ目。旧モデルは `pBase` が正答率の天井になっていて、
`6^9` は何分かけても 52% で頭打ちだった。手計算なら見直せるので、
時間さえあれば当たるようになるのが正しい。→ §12.3

数式を AST として辿り、ノードごとに次を積み上げる。

| 量 | 中身 |
|---|---|
| `time` | 基本演算の実行時間。乗算は**部分積の個数（桁数の積）**に比例、加算は**桁上がりの回数**を明示的に持つ。加えて「式制作＋回答入力の固定10秒」と「答えの入力（桁数に比例）」 |
| `trace` | 手順ごとの内訳。3段の入れ子（式操作/計算/答え入力 → 演算ごと → 筆算の1行ごと）でテスト場に表示する |
| `ok` | 間違えない確率。部分積1つあたりの取りこぼし率から積み上げる |
| `wm` | **Ershov 数**（式木の評価に必要な最小レジスタ数）× 値の桁数由来のチャンク数 |
| `sigma` | log10 の絶対誤差（桁数申告用。診断にのみ使用） |

依拠した知見:

- **作業記憶**: Miller (1956) 7±2、Cowan (2001) の「チャンク約4」→ `wmCapacity` を 3.5〜8.0 で段階化
  （素の暗算容量より上に取ってあるのは、紙が外部記憶として働くため）
- **レジスタ割り当て**: Ershov 数は「同時に覚えていなければならない中間結果の数」と構造的に同型
- **桁上がりコスト**: Fürst & Hitch (2000), Imbo et al. (2007) — 桁上がりが中央実行系を圧迫する
- **problem-size effect**: 大きなオペランドほど遅く誤りやすい
- **チャンク化 / 事実検索**: Chase & Simon (1973), Logan (1988) — 九九・小さい階乗・10のべきは*計算されず検索される*
- **課題切替コスト**: Monsell (2003) — 演算子が変わるたびに再設定コスト
- **速度‐正確性トレードオフ**: Wickelgren (1977) — 時間に対して漸近的に上がる正答率

木構造だけを見る Ershov 数では拾えない「1つの演算の内部での中間結果の連鎖」
（6^9 の 36→216→1296→…）は、演算ごとの `wmExtra` として別に足している。

#### 掛け算のコストは何秒か

紙の上で実際にやることに分けてある（`mulBreakdown()`）。

```
d1桁 × d2桁 の筆算 = 部分積が d2 行
  各行   : 九九を d1 回引く（MUL_TABLE）＋ d1+1 桁を書く（WRITE_PER_DIGIT）
  行が2つ以上あれば、それらを足す（ADD_PER_COLUMN × 答えの桁数 × (行数−1)）
  最後に答えを書く（WRITE_PER_DIGIT × 答えの桁数）
1桁×1桁 は九九の想起（MUL_TABLE = 0.80秒）＋ 答えを書く
```

この分解のおかげで、テスト場で `6^4 × 6^4` を開くと
「部分積1行目に5.8秒、繰り上がりの処理に4.7秒、答えの8桁を書くのに4.1秒」
まで下りて見られる。合計は分解前と同じ。

外部の根拠と突き合わせてある。

| | |
|---|---|
| 1桁×1桁 | 成人が「7×8」に答えるまで**約1秒**（Campbell & LeFevre 2001）。0.80秒はほぼ一致 |
| 4桁×4桁 | モデルは32秒。筆算は部分積4行×5桁＋合計8桁＝**28文字を書く**ので、成人の書字速度から見て**20〜40秒**が妥当な帯。その中ほど |
| 筆算中の九九 | **単発と同じ 0.80秒**。以前は「筆算は流れに乗っているぶん速い」として 0.44秒 に分けていたが、これは裏の取れていない仮定だったので統一した（下記） |
| 8桁×8桁 | モデルは約120秒。暗算の世界記録は平均17秒だが、あれは筆算ではなく交差乗算なので別物。遅い側に振れているのは安全 |

多桁の筆算そのものの秒数を測った研究は見つけられなかったので、
**中間の桁数は依然として判断値**。ただし上のように両端が押さえられている。

##### 九九の秒数は場面で使い分けない

以前は定数が2つあった。単発の九九 `MUL_TABLE = 0.80` と、筆算の途中で引く九九
`MUL_RECALL_PER_PARTIAL = 0.439`。後者を速くしていた理由は
「筆算は手順が流れに乗っているので毎回ゼロから思い出すわけではない」だったが、
**これは裏の取れていない仮定**で、しかも 0.439 という値自体はフィッターが
アンカーに合わせて動かしたものだった。つまり「速いはず」という直感に、
校正のしわ寄せを吸わせる自由変数を1つ与えていたことになる。

根拠があるのは「九九1回 ≈ 1秒」（Campbell & LeFevre 2001）だけなので、
**両方とも 0.80 に統一した**。速いという証拠が出たときに分ければよい。

影響は素直に出る。`6^9` の 大学生 は 61秒 → 69秒。
`6^9` の 4桁×4桁 の段が 25秒 → 32秒 になったのがほぼそのまま乗っている。

**この件がフィッターを捨てるきっかけになった。** 詳細は §12.6.1。
所要時間が伸びれば同じ時間での正答率は当然下がるのに、フィッターは
書字速度を下限（150文字/分）に張り付けてまで元の正答率に戻そうとした。
守っていたのはアンカーで、歪めていたのは根拠のある値のほうだった。

#### 操作時間は計算の速さで割らない

**式を組むD&D操作**は `SETUP_BASE + SETUP_PER_CARD × 置く枚数`。
括弧は常設ツールから取るだけなので半分の重みで数える。
標準的な4枚の式でちょうど10秒になるように置いてある。

| 式 | 置くもの | 式を組む |
|---|---|---|
| `9!` | カード2枚 | 7.7秒 |
| `6^9` | カード3枚 | 8.8秒 |
| `9+8*7` | カード5枚 | 11.0秒 |
| `(4+6)^9` | カード5枚＋括弧2個 | 12.1秒 |

画面の操作なので `profile.speed` では割らない
（競技者だからカードを1.6倍速くドラッグできるわけではない）。

**答えの入力**は `WRITE_PER_DIGIT × 答えの桁数`。
「全桁書き切れる範囲で最大の数を狙う」という設計の実効的な上限はここで決まる。

#### 階乗は誰も暗記していない

`factKnown` は 中学生〜大学生で **1**（＝何も暗記していない）、競技者4、トップ6。
そのぶん掛ける順番が効く。

```
順に掛ける（中学生・高校生）: 1×2, ×3, ×4, ×5, ×6, ×7   → 最後は 3桁×1桁 まで育つ
小さい順に畳む（大学生以上） : 2×3=6, 4×5=20, 6×7=42
                              → 6×20=120 → 120×42=5040   → 多桁の筆算が1回で済む
```

畳む手順を使えるのは `chainExponent` を持つプロファイル（大学生以上）。
7! の所要時間は 大学生 24秒 / 高校生 31秒 と、**手順の差がそのまま出る**。

#### 末尾の0は「計算しない」

手計算では末尾の0は書き足すだけで済む。値を `m × 10^k` に分解して扱う。

```
a × b  :  m_a × m_b を筆算し、k_a + k_b 個の0を書き足す
a ^ b  :  m_a^b を計算し、k_a × b 個の0を書き足す
          m_a == 1 のときだけ計算そのものが消える
```

**`m == 1` かどうかが決定的。** ここを「末尾が0なら全部タダ」と実装すると、
`30^7` が `10^7` と同じ値段になる（`3^7 = 2187` を求める手間が消えてしまう）。
`(4*5)^9` = 20^9 のようにゲーム内で実際に作れる手なので、ズルの穴になる。

### 12.3 時間プレッシャー と 検算

**計算に使える時間 = ポットのチップ数（秒）**。これを3つに分解して効かせる。
3つ目が手計算モデルの中核。

1. **間に合う確率** — 人の作業時間は対数正規に散らばるので `P(T < 制限時間)`
2. **焦り係数** — 間に合わせるために手順を省くぶん、初回の誤り率が指数的に増幅される。
   増幅の強さは `stressTolerance` で緩和される
3. **見直し** — 余った時間で検算できる。紙に手が残っているから可能な経路

```
見直し回数   n    = min(MAX_RECHECKS, floor((使える時間 − 所要時間) / (所要時間 × RECHECK_FACTOR)))
初回誤り     err1 = 1 − pSteps^焦り係数
系統誤り     syst = err1 × (SYSTEMATIC_BASE + SYSTEMATIC_SLOPE × 難易度)
見直し後     errN = syst + (err1 − syst) × (1 − checkRate)^n
正答率            = baseAccuracy × 間に合う確率 × (1 − errN) × pWm × pMode
```

検算で消えるのは**うっかりミス**だけ。手順の思い違い（`syst`）は何度なぞっても
同じ答えに着地するので残る。難しい式ほどその比率が上がる。

結果として `6^9` はこう動く。

| | 30秒 | 60秒 | 120秒 | 300秒 |
|---|---|---|---|---|
| 高校生（所要89秒） | 1% | 3% | 41% | 73% |
| 大学生（所要69秒） | 1% | 23% | 82% | 91% |

一方 `(4+6)^9` は所要25〜26秒なので、30秒の時点で 64〜69%、60秒あれば 98%。
**同じ「10億前後の数」でも、短時間で出せるかどうかがまるで違う**というのがこのゲームの肝。

見直し回数は300秒あたりで上限に達するので、そこから先はほとんど伸びない
（4回見直して見つからない誤りは、10回見直しても見つからない）。

### 12.4 答えの形式による制限（設計者の指示）

`engine.js` の `judgeDeclaration` は規模に応じて要求する答えを変える。

| モード | 境界 | 要求 | AIの正答率 |
|---|---|---|---|
| `exact` | 10進 **1000桁以下** | 値そのもの（全桁） | 難易度モデルどおり |
| `digits` | 〜10^(10^1000) | 桁数 | **0%** |
| `scale` | それ以上 | 10↑↑x の規模 | **0%** |

厳密以外は 0%（`COG.NON_EXACT_ACCURACY`）。結果として AI は
**「自分が全桁書き切れる範囲で最大の数」**を狙う打ち方になる。

> **未解決の設計課題**: `EXACT_MAX_DIGITS = 1000` は人間の限界よりはるかに上にある。
> `9^81`（78桁）や `9^99`（95桁）も厳密モードなので、78桁を全部書かされる。
> 実際に書き切れるのは10〜12桁程度なので、**12桁〜1000桁の帯が事実上使えない**。
> 難易度モデルはコストの発散として正しく扱う（＝AIはその帯を選ばない）が、
> ゲームとしては境界を下げる（例: 15桁）ほうが遊べる範囲が広がる。

### 12.5 テトレーションの制限

以下のみ許可。それ以外は `blocked = true` で **100%誤答**。

`b↑↑2`（右辺2なら左辺自由） / `2↑↑3` / `3↑↑3` / `2↑↑4`

許可されたものはすべて厳密モードに収まる（`9↑↑2 = 387420489`、`3↑↑3 = 7625597484987`、
`2↑↑4 = 65536`）ので、ゲーム内の最大桁数が人間の計算可能域に収まる。

### 12.6 AIレベル

人間の層に対応させてある。**内部IDは変えていない**（`models/policy_<id>.json` と
`HNP_LEVEL` が参照するため）。

| id | 表示 | 作業記憶 | 速度 | 4桁×4桁の初回誤り | 検算の発見率 | 暗記している階乗 | 暗記しているべき |
|---|---|---|---|---|---|---|---|
| `novice` | 初級（中学生） | 3.5 | 0.72 | 30% | 0.35 | なし | 2^5 / 3^3 |
| `casual` | 中級（高校生） | 4.5 | 0.88 | 18% | 0.48 | なし | 2^10 / 3^4 |
| `skilled` | 上級（大学生・理系） | 5.5 | 1.00 | 10% | 0.62 | なし | 2^12 / 3^5 |
| `expert` | 達人（競技者） | 6.5 | 1.30 | 6% | 0.74 | 4! | 2^16 / 3^7 |
| `master` | 超人（トップ競技者） | 8.0 | 1.60 | 3.5% | 0.82 | 6! | 2^20 / 3^9 |

**暗記の範囲は所要時間を直接決めるので、正答率と同じくらい効く。**
階乗を **誰も暗記していない**設定にしてあるのは、`7!` を
`7×6=42, 5×4=20, 3×2=6 → 840 → ×6 = 5040` のように *その場で作る* のが実態だから。
記憶から引く設定にすると `7!` も `8!` も `9*8` も揃って10秒になり、式の中身が見えなくなる。

| | `9*8` | `6!` | `7!` | `8!` | `9!` | `(4+6)^9` | `30^7` | `6^9` |
|---|---|---|---|---|---|---|---|---|
| 大学生の所要時間 | 12秒 | 21秒 | 28秒 | 37秒 | 47秒 | 25秒 | 47秒 | 69秒 |
| 高校生の所要時間 | 12秒 | 25秒 | 32秒 | 41秒 | 53秒 | 26秒 | 59秒 | 89秒 |

`slipRate` は「4桁×4桁の筆算1回を初回で間違える確率」から逆算している
（部分積16 + 最終加算4 = 20操作なので `slipRate = 1 − (1 − p)^(1/20)`）。
速度は**手を動かす速さ**なので、暗算ほど個人差が開かない（0.72〜1.60）。

上級以上は `a^9 = ((a^2)^2)^2·a` のような効率的なべき乗手順を使える（`chainExponent`）。
**正答率は上限 0.999**。100%正解には決してならない。

### 12.6.1 定数はどう決めたか（`tools/calibrate-ai.js`）

実測データは無い。**手で決めて、決めた理由をその定数のコメントに書いてある。**

以前は違った。「大学生は `6^9` を5分で92%当てるはず」といった**目標値（アンカー）**の表を置き、
`--fit` で定数を座標降下させて合わせにいっていた。これをやめた。

#### なぜアンカーに合わせるのをやめたか

**アンカー自体が根拠のない当て推量だから。** 実測ではなく設計者の感覚で置いた数字に
モデルを合わせにいくと、次の3つが起きる。

1. アンカーが間違っていても、定数がそれを吸収して見た目は合う
2. 外部の根拠がある定数（九九1回 ≈ 1秒）まで押し出される
3. どの定数が「根拠のある値」でどれが「辻褄合わせ」か区別がつかなくなる

実際に起きた。筆算中の九九を 0.80秒 に統一したら `6^9` の所要が 61秒 → 69秒 に伸びた。
これは**そうあるべき変化**（手計算はもっと遅いはず、という判断で伸ばした）なのに、
フィッターは `WRITE_PER_DIGIT` を下限 0.40（＝150文字/分）に張り付けて
元の正答率に戻そうとした。**アンカーを守るために、根拠のある値のほうを歪めていた。**

境界に張り付く定数は「当たっている」のではなく**ズレを吸っている**。
それが起きた時点で、フィッターは自由変数を配る装置でしかない。

#### 今の役割 — 「合わせにいく」から「変化に気づく」へ

ツールを2つに分けた。

| | 何を見るか | 破ったら |
|---|---|---|
| **要件**（`REQUIREMENTS`） | 守らないと**ゲームが壊れる**性質。定性的 | `exit 1` |
| **記録**（`SNAPSHOT`） | いまモデルが出す値。**目標ではない** | 差分を表示するだけ |

要件に書くのは構造だけで、具体的な正答率は書かない（書くとアンカーに戻る）。

- 強いプロファイルほど当てる / 速い
- 時間を足せば正答率は下がらない
- `(4+6)^9` は `6^9` よりはっきり速い（**値の大きさではなく計算過程で難易度が決まる**という看板）
- `30^7` は `10^7` より高い（末尾0バグの回帰テスト）
- `9*8` / `7!` / `8!` / `9!` が同じ秒数に潰れない
- 九九1回 = 0.80秒、書字 50〜150文字/分、4桁×4桁の筆算 20〜45秒

数値の帯は**明らかにおかしい値を弾くためだけの広さ**にしてある。狭めるとアンカーと同じになる。

記録との差は**それ自体は失敗ではない**。意図した変更なら `--snapshot` で貼り替える。
意図していない差が出たら、直近でいじった定数を疑う。

```bash
node tools/calibrate-ai.js             # 要件の検査 ＋ 現在値の一覧
node tools/calibrate-ai.js --curves    # 時間ごとの正答率カーブも出す
node tools/calibrate-ai.js --check     # 要件を破ったら exit 1（CI 用）
node tools/calibrate-ai.js --snapshot  # 記録に貼る値を出力する
```

**`COG` や `AI_PROFILES` をいじったら必ずこれを通すこと。**

#### 要件が落ちたとき、疑うのはまず要件のほう

この作り替えの直後、`30^7 は 10^7 より重い` が競技者とトップで落ちた。
調べたらモデルのほうが正しかった —— 競技者は `3^7 = 2187` を暗記しているので、
`30^7` は想起1回＋0を7個書くだけで本当に安い。要件の書き方が雑だっただけだった
（暗記していないプロファイルにだけ大きな差を求めるよう直した）。

**モデルを直す前に、要件が本当に要件なのかを疑う。** これが以前と逆になった点。

### 12.7 誤答時の誤差分布

難易度 `d` が高いほど盛大に外す。段階と重み:

| 段階 | 内容 | 重み |
|---|---|---|
| `slip` | 1桁だけ書き間違える / 隣を入れ替える / 桁数±1 | (1−d)² |
| `small` | 数%〜十数%のずれ | 2d(1−d) |
| `medium` | 2〜10倍 | d |
| `large` | 10^1〜10^3 | d² |
| `huge` | 桁が丸ごと違う | d³ |

`slip` は「1桁だけ違う」「隣り合う桁が入れ替わる」という*実際に人がやるミス*を作る。

### 12.8 数式の探索（計算コスト）

括弧を除いて5枚まで。数字 n 個を二項演算で繋ぐには n−1 個の演算子が要るので
`2n−1 ≤ 5`、つまり **数字は最大3枚**。形は次の3通りしかない。

- 数字1 + 後置 `!` を最大4個
- 数字2 + 二項1 + `!` を最大2個（左/右/全体へ配置）
- 数字3 + 二項2（ちょうど5枚）

実測で1手札あたり列挙 **104本**、全解析込みで **約3ms**。枝刈りより先に全部見るほうが速い。

そこから **(値, 難易度) のパレート境界**を取る。値の降順に見て、
「それより大きい値を出す候補すべてより易しい」ものだけを残す。
結果、残るのは*リスクとリターンのトレードオフになっている候補だけ*（平均8.3本）。

### 12.9 ベット額をどう決めるか（`decideBet`）

3つの力の釣り合いで決める。1つでも欠けると壊れる。

| | 効き方 | 無いとどうなるか |
|---|---|---|
| **計算時間**（設計指示7） | ポットが増えると計算時間が伸び、自分の正答率が上がる。**難しい式を持っているほど賭けたい** | ベットが式の中身と無関係になる |
| **フォールドエクイティ** | 大きく賭けるほど相手は降りる。全員降りればショーダウン抜きでポットを取れる | **ブラフが成立しない**。賭けるほど得になり全部突っ込む |
| **トーナメントの価値（ICM）** | チップではなく順位の期待値で測る。全部失えばそこで終わり | 破産のコストが 0 になる |

このゲーム特有の点として、**計算時間は卓で共通**なので、大きく賭けると
受けた相手の正答率も上がる。普通のポーカーには無い、ブラフに固有のコスト。
`callProbability()` にこの引き戻しが入っている。

#### 3つの結末を ICM で評価する

```
全員降りた      : +pot                       （ショーダウン無しでポットを取る）
受けられて勝った: +(最終ポット − 自分の追加額)
受けられて負けた: −自分の追加額               （0 になればその場で最下位確定）
```

```
score = P(全員降りる) × ICM(奪う)
      + (1 − P(全員降りる)) × [ 勝率 × ICM(勝つ) + (1−勝率) × ICM(負ける) ]
```

降りるかどうかは「一番良い賭け方をしても、いま降りるより score が低いか」で決める。
ポットオッズの比較を ICM に置き換えたもので、**飛ぶリスクがここに入っている**。

ブラフは `bluffRate` で振り、当たったハンドは「相手は降りる」を強気に見積もる
（`BLUFF_OPTIMISM`）。弱い手ほど大きく賭ける方向に効くので、これがブラフになる。
以前のように fold 判定をランダムに無効化するのではない。

> **`callProbability` の数字には根拠が無い。** 相手の手札は見えないし、
> このゲームの「降り方」の実測も無い。形だけ理屈で決めて、値は判断で置いている。
> `BET_MODEL` にまとめてあるので、実データが取れたら差し替えられる。

#### 直したバグ: 毎ハンド全部突っ込んでいた

以前の式はこうだった。

```
finalPot = pot + extra × (1 + 相手の人数 × 0.55)
ev       = 勝率 × finalPot − (1 − 勝率) × extra
```

`extra` についての傾きが `勝率 × (1+人数×0.55) − (1−勝率)` なので、3人卓では
**勝率が 0.323 を超えた瞬間に「賭けるほど得」**になり、常にオールインが最大になる。
「相手は必ずベットの55%ずつ付いてくる」という仮定のせいで、
**ベットを増やすことの不利益がモデルに存在しなかった**。

実測（各400ハンド、ヒューリスティック方策）:

| | 中学生 | 高校生 | 大学生 | 競技者 | トップ |
|---|---|---|---|---|---|
| オールイン率（前） | 20.6% | 44.7% | **78.3%** | **84.6%** | — |
| オールイン率（後） | 3.2% | 7.4% | 11.8% | 15.7% | 14.8% |
| レイズ率（前） | 0.2% | 0.1% | 0.2% | 0.4% | — |
| レイズ率（後） | 7.5% | 34.4% | 39.5% | 38.6% | 37.6% |

強いプロファイルほど突っ込んでいたのは、勝率が高いぶん早く 0.323 を超えるため。
レイズ3段階が 0.2% しか使われず、行動空間が実質 {fold, call, allin} に潰れていた。

直したあと、賭け額が手の強さに追従するようになった（大学生・ブラフ無効）:

| 手の強さ | 0.00–0.30 | 0.30–0.45 | 0.45–0.55 | 0.55–0.65 | 0.65– |
|---|---|---|---|---|---|
| 積んだ額 / 自分のスタック | 15.1% | 19.7% | 27.3% | 34.8% | 38.5% |
| オールイン率 | 2.6% | 1.6% | 5.2% | 10.5% | 13.2% |

#### 相手の読み（観測に足した）

**長いあいだ AI は相手を一切見ていなかった。** 観測に入っていたのは
`toCall/pot` や `currentBet/bb` という合算値だけで、
「誰がオールインしたか」「何人がレイズを返したか」は入力されていない。
そのため **PPO を何世代回しても相手の読みは学習しようがなかった**。

`js/ai-policy.js` に12個足して `OBS_DIM` を **73 → 85** にした。

| # | 特徴量 | なぜ要るか |
|---|---|---|
| 1–3 | このラウンドでレイズ / オールイン / 降りた相手の割合 | 場が荒れているのか穏やかなのか |
| 4–5 | 直前の仕掛けがレイズか / オールインか | 直近の圧力 |
| 6 | 直前の仕掛けの大きさ ÷ そのときのポット | ポット比なのでブラインドの上がり方に依存しない |
| 7–8 | 相手の最大 / 最小スタック ÷ 自分のスタック | 誰に対して危ないのか |
| 9 | 自分より短いスタックの相手の割合 | トーナメントでは「先に飛ぶ人が居る」ことに価値がある（ICMの効き方が変わる） |
| 10 | 自分の後ろにまだ行動していない相手の割合 | ポジション |
| 11 | 自分のスタック ÷ 場の全チップ | トーナメント全体での立ち位置 |
| 12 | まだハンドに残っている相手の割合 | 何人と勝負しているか |

**席ごとの値をそのまま並べない。** 人数が 2〜6 で変わると観測の長さが変わってしまうので、
**割合に畳んで固定長**にしてある。誰がやったかは落ちるが「何人が / どれくらいの大きさで」は残る。

これを載せるために `js/game.js` に行動履歴を持たせた
（`player.lastAction` / `player.lastActionRatio` / `game.lastAggressorIdx`、
ベットラウンドの頭でリセット）。それまで「誰が何をしたか」はログの**文字列**にしか
残っておらず、観測に載せようがなかった。

> **ヒューリスティック方策のほうは依然として相手を見ていない。**
> `OPPONENT_MODEL` は固定のCDF1本で、ハンドが変わっても更新されない。
> `bluffRate` はコイン投げであって「相手が弱そうだから仕掛ける」ではない。
> 読みを持てるのは学習した方策だけ。

### 12.10 テストプレイ場（`ai-lab.html`）

§12 のモデルを**ゲームを回さずに直接叩く**ための画面。`/lab` または `ai-lab.html`。

やることは2つだけ。

| タブ | 入力 | 出力 |
|---|---|---|
| 式テスト | カードを並べて式 ＋ 計算時間（秒） ＋ 難易度 | 正解値・答え方・理論正答率・実測正答率・CPUの申告サンプル・内訳・5レベル比較・時間スイープ |
| 手札テスト | 手札7枚 ＋ 難易度 ＋ **仮想の卓**（手入力 or AIまかせ） | **1ハンドを最後まで回して**、第1ラウンドのベット → 交換 → 第2ラウンドのベット を4つのサブタブで見る |

#### 仮想の卓 —— 1ハンドを最後まで回す

手札だけ見ても「で、こいつはどう打つのか」が分からない。しかも1点では決まらない。

```
第1ラウンドで賭ける → 交換して手札が変わる → 第2ラウンドで賭け直す
```

この流れ全体を見ないと、交換の良し悪しも降り引き際も分からないので、
**本物の `Game` を1ハンド分まわす**。相手も同じ AI（`decideBet` / `decideExchange`）で打つ。
ラボ専用のロジックは書かない（書くと「ラボでは動くが本編では違う」の温床になる）。

卓の作り方は2通り。

| | 何を決めるか |
|---|---|
| **自分で決める** | 第1ラウンドの開始局面（ポットとコール額）を手入力。そこから先は相手もAIが打つ |
| **AIにまかせる** | アンティとブラインドから始めて、相手のベットも交換も全部AIが決める。状況を考えるのが面倒なとき用 |

#### シーンを分ける（手札タブの中のサブタブ）

縦に全部並べると1画面に収まらず、どこを見ればいいのか分からなくなるので4つに割った。

| サブタブ | 中身 |
|---|---|
| ① 交換前のベット | 第1ラウンドの判断と、その内訳 |
| ② カード交換 | 捨てる札 / 引いた札 / 交換で良くなったか |
| ③ 交換後のベット | 第2ラウンドの判断。ここまでの進行ログつき |
| ④ 候補と比較 | 候補一覧のパレート境界、レベルごとの選択。**①③ と同じ計算時間**で見る |

##### ① ③ ベットの判断

- **ベット判断**（フォールド / チェック / コール / レイズ額 / オールイン）
- **ベット額ごとの中身** —— 選ばれなかった額も全部。
  出す額・その額でのポットから決まる計算時間・相手が全員降りる確率・
  その時間で選ぶ式・正答率・beat・勝率・score（ICM）。
  **降りたときの score も併記**するので、なぜ降りたかが読める
- **降りる理由の書き分け** —— 「作れる式が弱い」と「手は悪くないがオッズが合わない」と
  「オッズは合うが飛ぶリスクが勝つ（ICM）」は対処が別物なので混ぜない
- **作れる式の評価** —— 効用 = pCorrect × beat のどちらが足を引っ張っているかで
  **難しすぎる / 弱すぎる / 両方** に分ける。前者は易しい式へ、後者は大きい式へと
  対処が正反対なので、「手が弱い」の一語にまとめない
- **降りる場合でも、続けていたら作る予定だった式とカード** を表示する

第1ラウンドで降りると ② ③ が消えてしまう。交換とその後を見るのが目的なので、
**そこだけは降りを打ち消してコールで進め、そのことを画面に明示する**。
第2ラウンドの降りはそのまま尊重する（先を見る必要が無いため）。

##### ④ 候補と比較 —— 秒数を手で入れさせない

ここだけ「条件」欄の秒数（既定60秒）を使っていて、
**③ が10分で考えている式と ④ が並べる式が食い違っていた**。

計算時間は**ポットから決まる**もので（設計指示7）、手で入れる数字ではない。
手札タブから秒数の入力欄を外し、**卓を回した結果の計算フェーズの制限時間**を使う。
そこまで進まなかったときは直近のベットからの見積もりで、どちらを使ったかは画面に出す。

「交換前の手札 / 交換後の手札」は切り替えられる。**時間は同じまま**なので、
交換で手が良くなったのかを同じ物差しで比べられる。
好きな秒数で試したいときは式テストタブへ（そちらは時間スイープも出る）。

##### ② カード交換

捨てる札と引いた札を、**手札の並びを変えずに**色で示す（並べ替えるとどれが動いたのか
追えなくなる）。引き直しは `Game.readyExchange()` がそのまま山札から引くので、
乱数シードを変えると引きが変わる。

交換で手が良くなったのかを、**同じ計算時間で**交換前後の最良候補を比べて出す。

判断の内訳を出すために `decideBet` を
`evaluateBetSizes()`（全部の額を評価）と `resolveBetChoice()`（Game が受け取る形に落とす）に
分けてある。`decideBet` はこの2つを呼ぶだけ。
交換もこの `evaluateBetSizes()` の結果から計算時間を取る（`plannedCalcTime()`）ので、
**ベットと交換は同じ数字を見ている**。

##### ラボを作って見つかったもの

**1. カードIDの衝突（修正済み）**

`buildDeck()` は呼ぶたびに id を `n0` から振り直す。
ラボで配った手札と `Game` の山札が**同じ id を持つ**ので、
「引いた札」を id の差分で判定すると取りこぼす。
実際「4枚捨てたのに3枚しか引いていない」表示になった。
手札を卓に入れるときに id を振り直して解決。
ついでに、自分が持っている札と同じものを山札から抜くようにした
（抜かないと9を4枚持ったまま5枚目の9を引けてしまう）。

**2. 交換とベットで見ている式が違った（修正済み）**

`decideExchange` は `expectedCalcTime(game, 0)` を使っていた。つまり
**今のポットのまま**計算時間を見積もる。だが計算時間はポットで決まり、
ポットは計算フェーズに入るまで増え続ける。時間を短く見積もると
易しくて小さい式を高く評価するので、こうなっていた。

```
交換  : 今のポット      →  1分0秒 で (8^9)+6 を守る
ベット: 賭けた後のポット → 10分0秒 で (9+6)^8 を狙う
```

**守る札と狙う式が食い違う。**

直し方は `plannedCalcTime()`（`js/ai.js`）。
「計算フェーズのポットはいくらか」を当てにいくと**また根拠のない判断値が増える**ので、
そうはせず **AI 自身のベット計画から取る**。`evaluateBetSizes()` は既に
額ごとの「その額を出したときのポットと計算時間」を出しているので、
その最良手の `calcTime` をそのまま使う。

これでベットと交換が**定義上同じ数字**を見ることになり、食い違いようがなくなる。
**増えた定数はゼロ。**

検証: 自己対戦500ハンド・交換フェーズ892件で、
「守る式」と「ベットするなら狙う式」が **892/892 一致**（計算時間も完全一致）。
テスト場の12シード＋おまかせ20シードでも不一致は出ない。
不一致の検知はテスト場に残してある（出たらモデルが壊れたサイン）。

**残っている過小評価**: どちらも「このベットラウンドが終わった時点のポット」までしか見ない。
交換の後にもう1ラウンドあるぶんは、まだ数えていない。
埋めるには「次のラウンドでいくら入るか」の仮定が要り、それは根拠の無い定数になるので入れていない。

速度への影響: 交換のたびに `evaluateBetSizes` を1回余分に呼ぶので、
自己対戦のスループットが 240 → 200 ハンド/秒（約17%減）。要件に対しては3桁の余裕がある。

正答率を**2通りで出す**のは実装どうしを突き合わせるため。

```
理論値 = accuracyUnderTime(analysis, 秒, profile).pCorrect
実測値 = produceSubmission() を N 回 ÷ 当たった回数
```

さらに申告文字列を `FormulaEvaluator.judgeDeclaration()` に通し直し、
`produceSubmission` が「正解」と言った申告が本当に判定を通るかも数えている
（食い違えば画面に警告が出る＝モデルの不整合）。

`js/ai-lab.js` は **ゲームロジックを一切持たない**。
engine.js / ai-cognition.js / ai.js の公開APIを呼ぶだけにしてある。
ここで独自に計算を書くと「ラボでは通るが本編では違う」が起きるため。

チューニングパネルから `COG` の定数をその場で書き換えられる（メモリ上のみ。ファイルには保存されない）。
書き換えたら `AICognition.clearAnalysisCache()` と `AI.clearCaches()` を呼んでキャッシュを捨てている。
良い値が見つかったら `js/ai-cognition.js` の `COG` を手で直す。

---

## 13. AIの学習（`train/`）

### 13.1 何を学習し、何を学習しないか

| | |
|---|---|
| **学習する** | ベット（降りる/コール/レイズ3段階/オールイン）、どの候補式を出すか、何枚交換するか |
| **学習しない** | 正答率のモデル（§12）。人間を模した**固定の物理法則**として扱う |

正答率まで学習させると「間違えないAI」に収束してゲームが成立しない。

### 13.2 構成

```
Python (train.py)  ──PPO──┐
                          │  stdin/stdout の NDJSON
Node (env_server.js) ─────┘  N卓を並列に回す
```

巨大数エンジンを Python に書き直すと**学習環境と実ゲームがズレる**という
一番タチの悪いバグを抱える。ルールの正は `js/engine.js` ただ1つに保つ。

- 1エピソード = **1ハンド・1席**（PPO のロールアウト単位）
- 割引なし（γ=1）、優位性は `A = R − V(s)`
- 観測85次元（状況16 ＋ **相手12** ＋ 候補6×9 ＋ まとめ3）、行動は3ヘッド（bet 6 / formula 6 / exchange 6）＋合法手マスク

### 13.3 報酬 —— なぜ bb/hand をやめたか

**このゲームはトーナメントで、キャッシュゲームではない。**
実ゲームは1人を残して全員が飛ぶまで続き、途中でチップを買い足せない。
にもかかわらず学習側は長らく「1ハンド打って収支を数え、飛んだらリバイ」で回していた。
これは**別のゲームを学習していた**ということで、次の2つがズレる。

| | キャッシュゲーム（旧） | トーナメント（現） |
|---|---|---|
| チップの価値 | 常に線形。1000点は 1000点 | **逓減する**。手持ちが増えるほど追加1点の価値は下がる |
| 破産 | リバイすれば元通り | **そこで終わり**。取り返す機会が消える |

この差が効くのは**降りる判断**。線形なら「期待値がわずかにプラスならコール」が正しいが、
トーナメントでは飛ぶ側のダメージが大きいので、同じ期待値でも降りるのが正しい場面がある。
リバイ付きで学習させると、AIはそれを一切学ばない。

#### ICM（Independent Chip Model）で1ハンドごとに報酬を作る

トーナメントの本当の報酬は**順位**だが、それは数十ハンド後にしか決まらない。
そのままでは報酬が遅すぎて学習しない。かといって PPO のロールアウト構造
（1ハンド＝1エピソード）を崩したくない。

そこで**スタックを順位期待値に換算**し、その差分を毎ハンドの報酬にする。

```
reward = ICM(ハンド後のスタック) − ICM(ハンド前のスタック)
```

ICM は「各席が優勝する確率はスタックに比例する」という仮定から順位分布を出す
標準的なモデルで、`icmEquity()` が全順列を歩いて厳密に計算している（席数 ≤ 6 なので十分速い）。
賞金は `payout[k] = 1 − 2k/(n−1)`（優勝 +1、最下位 −1）のゼロサム。

これは **potential-based reward shaping**（Ng, Harada & Russell 1999）そのもので、
毎ハンドの報酬を足し合わせると**トーナメント全体の順位報酬にぴったり一致する**。
密な報酬を得ながら、最適方策は「順位を最大化する方策」から動かない。

数値で確かめた挙動:

| スタック | ICM | 意味 |
|---|---|---|
| `[1000, 1000, 1000]` | `0 / 0 / 0` | 対称なら全員フラット |
| `[2000, 500, 500]` | `+0.60 / −0.30 / −0.30` | ゼロサム |
| `[1400, 1400, 200]` | `+0.375 / +0.375 / −0.75` | 短いスタックの不利が大きい |

`1000 → 1200` は +0.22 だが `2000 → 2200` は +0.16。
**同じ200点でも、持っているほど価値が下がる**。これがトーナメント特有の判断を生む。

#### 環境側の変更

- `reset()` が**トーナメント1つ**を作る。決着したら次のトーナメントを始める
- `rebuy()` は**削除**した
- `levelUpHands: 5`（旧 0）。ブラインドが上がらないとトーナメントが終わらない
- `MAX_TOURNAMENT_HANDS = 300` で打ち切り（そこまでのスタック順で順位確定）。
  実測では `truncated_rate = 0` で、平均 5.5 ハンドで決着している

### 13.4 指標 —— bb/hand は主指標にならない

bb/hand は**生存バイアス**を持つ。トーナメントでは短いスタックほど強制的に
オールインさせられ、勝ち残る局面では大きなポットを取るので、
「早く飛ぶが飛ぶまでは派手に勝つ」方策の bb/hand が高く出てしまう。
逆に手堅く勝ち残る方策は bb/hand が 0 付近に落ち着く。

主指標は**勝ち残ること**にする。

| 指標 | 意味 |
|---|---|
| **優勝率** | 主指標。3人なら 1/3 = 0.333 が互角のライン。`vs 過去最強` と `vs ランダム` の両方を出す |
| **平均順位** | 1位が 1.0。優勝率だけだと「優勝か即死か」の方策を見逃す |
| `icm_per_hand` | 学習が最適化している当のもの。自己対戦ではゼロサムなので 0 付近 |
| `hands_per_tournament` | トーナメントの長さ。極端に短ければ全員が突っ込みすぎている |
| `truncated_rate` | 300ハンドで決着しなかった割合。0 でないならブラインド設定を疑う |
| bb/hand | 補助。上記の理由で単独では読まない |

過去最強チェックポイントの更新条件も `bb/hand > 0` から
**`優勝率 > 1/席数`** に変えた（`train.py`）。

#### 評価を「何ハンド回したか」で打ち切ってはいけない

**この作りで一度壊れた。** 直したので経緯を残す。

評価は `EVAL_HANDS = 600` ハンド回して終わりにしていた。ところが
**AIが強くなると1トーナメントが長くなる**（実測で 4.6 → 24 ハンド）。
32卓を並列に回して合計600ハンドだと1卓あたり19ハンドで、**1つも決着しない**。

決着ゼロなら優勝率は計算できないのに、`env_server` が `0` を返していたので
画面上は「**優勝率 0%**」に見えた。「測れなかった」と「全部負けた」の区別がつかない。

しかも `優勝率 > 1/席数` が過去最強の更新条件で、方策の書き出し
（`models/policy_*.json`）もその中にある。つまり:

```
AIが強くなる → トーナメントが伸びる → 決着ゼロ → 優勝率0 と表示
            → 更新条件を満たさない → 過去最強も policy_*.json も凍る
```

**強くなるほど記録が止まる**という、いちばん気づきにくい向きの故障だった。
40世代回したのに書き出されていたのは15世代目のもの。

直したのは3か所。

| | |
|---|---|
| 打ち切り条件 | ハンド数 → **決着したトーナメント数**（`EVAL_TOURNEYS = 60`）。ハンド数の上限は暴走よけにだけ残す |
| 測れないときの値 | `0` → **`null`**。`env_server` の `champion_rate` / `avg_place` も同様。0 を返すと負けと区別がつかない |
| 更新条件 | 決着が足りなければ**更新しないうえで警告を出す**。「0点として扱う」にすると静かに止まる |

`tourneys_vs_best` / `tourneys_vs_random` をログに残すようにしたので、
**その数字が何件で測られたのか**が後から分かる。

> 教訓として一般化すると: **「データが無い」を 0 で表すな。**
> 平均順位が `0` になっていたのが唯一の手がかりだった（順位は最小でも 1.0 なので、
> 0 はあり得ない値）。優勝率だけ見ていたら気づけなかった。

### 13.5 実測

| | |
|---|---|
| 環境のみ（ランダム方策） | 695ハンド/秒（1ハンド 1.4ms） |
| 学習込み | 約3,400 意思決定/秒、1世代（8192決定）2.3秒 |

要件「1アクション数秒以内・1ハンド1分程度」に対して3桁の余裕がある。

### 13.6 学習結果（上級プロファイル・40世代）

観測に相手の行動を足し、報酬を ICM にした後の結果。3人卓。

| 世代 | 申告正答率 | フォールド率 | 優勝率 vs ランダム | 平均順位 vs ランダム | 1試合の長さ |
|---|---|---|---|---|---|
| 5 | 0.73 | 0.28 | 0.64 | 1.61 | 5.0 |
| 10 | 0.82 | 0.31 | 0.74 | 1.48 | 9.8 |
| 20 | 0.82 | 0.33 | **0.84** | **1.24** | 17.5 |
| 30 | 0.77 | 0.25 | 0.70 | 1.53 | 21.1 |
| 40 | 0.80 | **0.15** | 0.72 | 1.51 | 22.9 |

3人卓の互角は 優勝率 0.333 / 平均順位 2.0 なので、ランダム相手には大きく上回っている。
`優勝率 vs 過去最強` は 0.27〜0.47 を行き来していて、自己対戦の均衡に達している。
`truncated_rate` は全世代 0（300ハンドで必ず決着）。

**1試合の長さが 5.0 → 22.9 ハンド**に伸びたのが、報酬をICMにした効果がいちばん見える所。
早々に突っ込んで飛ぶのをやめ、勝ち残る方向へ動いた。

#### 相手の特徴は効いたのか（対照実験）

足した特徴が本当に効いたのかは、**潰した対照と比べないと言えない**。
`HNP_ABLATE_OPPONENT=1` で相手特徴12個を全部 0 にしたまま、同じ設定で40世代学習させた。
次元は変えていないので、両者を同じ卓で戦わせられる。

**まず、ランダム相手では差が出なかった。** 世代20〜40の平均:

| 指標 | 相手特徴あり | 潰した対照 | 差 |
|---|---|---|---|
| 優勝率 vs ランダム | 0.756 ±0.048 | 0.746 ±0.046 | **+0.010** |
| 平均順位 vs ランダム | 1.432 ±0.110 | 1.436 ±0.086 | −0.004 |
| フォールド率 | 0.246 ±0.059 | 0.325 ±0.039 | **−0.079** |

優勝率の差 +0.010 は世代間のばらつき ±0.048 に埋もれている。**効果なしと読むべき。**

考えてみれば当然で、**ランダムな相手の行動には情報が無い**。
何をレイズと解釈しようが、相手の手札とは無相関なので読みようがない。
「相手を読む特徴を足したのにランダム相手で強くならない」のは、
特徴が無意味なのではなく**テストが不適切**だった。

そこで**直接対決**させた。同じ卓に「相手特徴あり」と「潰した対照」を置き、
席の有利不利を消すため3席すべてで回した。潰した側には学習時と同じく
相手特徴を 0 のまま渡す（見たことのない入力を渡すと不当に不利になるため）。

| 「相手特徴あり」を置いた席 | 優勝 |
|---|---|
| 席0 | 48 / 120 |
| 席1 | 50 / 120 |
| 席2 | 53 / 120 |
| **合計** | **151 / 360 = 41.9%** |

互角は 33.3%。**+8.6pt、95%信頼区間 ±5.1pt** なので有意。3席とも同じ向きに出ている。

**行動が情報を持つ相手にだけ効く**、という当たり前の結論で、
それは相手の読みが機能している証拠でもある。

> 教訓: 「相手を読む」機能をランダム相手で評価してはいけない。
> 読む対象が無い相手に対しては、原理的に効きようがない。

#### 参考: 旧目的（リバイ付き・bb/hand 報酬）での25世代

> 以下は §13.3 の報酬変更より**前**の記録。観測も73次元だった頃のもの。
> トーナメントの判断は学習できていないが、
> 申告正答率や提出値の規模といった「式の選び方」の傾向は報酬の定義に依らないので、
> そこは引き続き参考になる。

| 世代 | 申告正答率 | 決着率 | 提出値の規模(slog) | エントロピー | vs ランダム(bb/hand) |
|---|---|---|---|---|---|
| 1 | 0.32 | 0.70 | 2.05 | 1.57 | — |
| 12 | 0.63 | 0.89 | 1.48 | 1.15 | +28.3 |
| 25 | **0.74** | **0.95** | 1.39 | 0.97 | +19.8 |

AIは「**大きさを少し諦めて、確実に当てられる式を選ぶ**」方向へ収束した。
この傾向は新しい学習でも同じ（申告正答率 0.80 / slog 1.77）。

### 13.7 学習ダッシュボード（GUI）

**`AI学習コントロールパネル.vbs` をダブルクリック**（Windows）。
`python train/launcher.py` でも同じものが開く。Flask + SSE、画面は `train/web/`。

- ディスクの JSON ログを**正**とし、標準出力のパースは進行中の分を補うだけ
- グラフは**単系列 × small multiples・単軸**。2軸グラフは作らない
- 指標はレベルごとに別ファイル（`train/models/ppo_<レベル>_log.json`）

#### 起動まわりの3つの罠（`skills/ai-training-dashboard/references/` に詳細）

いずれも**静かに壊れる**ので、触るときは必ず思い出すこと。

| | 何が起きるか |
|---|---|
| `.vbs` を UTF-8 で保存 | Windows Script Host が ANSI として読み、日本語コメントが化けて次の行のコードを巻き込む。**エラーも出ずに何も起動しない**。UTF-16LE(BOM付き) で保存する |
| `.bat` に日本語（UTF-8）＋ `chcp 65001` | cmd.exe はファイル位置をバイト単位で追うので、コードページが変わるとマルチバイト文字で同期を失い、**行の途中から実行する**。`'...' is not recognized as an internal or external command` が延々出る。`AI学習コントロールパネル.bat` は **ASCII のみ**にし、日本語は `launcher.py --console` に寄せた |
| テンプレートの `<AppName>` を置換し忘れ | Windows はファイル名に `<` `>` を使えないので `os.makedirs` が WinError 123 で落ちる。しかも**サーバーは起動済み**なので「動いているのに窓が出ない」になる。`APP_DIR` に集約した |
| `.ps1` を BOM 無しの UTF-8 で保存 | PowerShell 5.1（Windows 標準）は BOM が無い `.ps1` を **ANSI(cp932) として読む**。日本語が化ける。UTF-8 BOM 付きで保存する（`share-internet.ps1` / `allow-firewall.ps1`）|

この3つは**すべて「日本語をどこに置くか」の問題**で、ファイル形式ごとに正解が違う。

| 形式 | 日本語を書けるか | 保存形式 |
|---|---|---|
| `.vbs` | 書ける | **UTF-16LE + BOM** |
| `.ps1` | 書ける | **UTF-8 + BOM** |
| `.bat` | **書かない** | ASCII のみ。日本語は `.js` / `.py` に逃がす |
| `.js` / `.py` | 書ける | UTF-8（BOM不要）|
| `pythonw.exe` で `print()` | コンソールが無いと `sys.stdout` が `None` になり、`print()` が例外で即死。画面には何も出ない。`launcher.py:_ensure_streams()` がログファイルへ逃がしている |
| 空きポート判定に `SO_REUSEADDR` | Windows では使用中のポートにも bind できる（Linuxと逆）。二重起動する。`find_free_port()` では付けていない |

`.vbs` はコンソールが無いぶん「ダブルクリックしても無反応」になりやすいので、
`launcher.py:check_dependencies()` が **flask / torch / numpy / node** を起動前に確かめ、
足りなければ Windows のダイアログで知らせる。
原因を詳しく見るときは `AI学習コントロールパネル.bat`（コンソールあり・その場で pip install できる）。

### 13.8 学習した方策をゲームで使う

`train.py` が過去最強を更新するたび `models/policy_<レベル>.json` を書き出す。
ゲーム側（`js/ai-policy.js`）が読み込み、そのレベルの CPU の行動選択に使う。

読み込み時に**観測次元を照合**し、合わなければ警告して無視する
（特徴量を変えたら重みは互換性を失うため）。無ければヒューリスティック方策で動く。

---

## 14. この作業で見つけたゲーム本体のバグ（修正済み）

| # | 内容 | 影響 |
|---|---|---|
| **B-1** | **全員フォールドしたとき、最後の1人がポットを取れなかった** | `_calculateShowdown` が「数式未提出」を失格として扱い、勝者0人になっていた。ポーカーの基本ルールが抜けていた。自己対戦で収支がゼロサムにならないことから発覚 |
| **B-2** | 勝者なしのポットの扱い | ルール改定により、持ち越しではなく**払い戻し（全員スコア0の同点）**に変更 |
